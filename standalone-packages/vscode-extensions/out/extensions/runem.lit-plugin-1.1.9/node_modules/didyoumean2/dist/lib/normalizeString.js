"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var lodash_deburr_1 = __importDefault(require("lodash.deburr"));
var R = __importStar(require("ramda"));
var trimSpaces = R.compose(R.replace(/\s+/g, ' '), R.trim);
/**
 * Normalize a string
 * @param {string} str - any string
 * @param {Object} options - options that allows you to modify the behavior
 * @returns {string} - normalized string
 */
var normalizeString = function (str, options) {
    return R.compose(function (s) {
        if (options.caseSensitive)
            return s;
        return R.toLower(s);
    }, function (s) {
        if (!options.deburr)
            return s;
        return lodash_deburr_1.default(s);
    }, function (s) {
        if (!options.trimSpaces)
            return s;
        return trimSpaces(s);
    })(str);
};
exports.default = normalizeString;
//# sourceMappingURL=normalizeString.js.map