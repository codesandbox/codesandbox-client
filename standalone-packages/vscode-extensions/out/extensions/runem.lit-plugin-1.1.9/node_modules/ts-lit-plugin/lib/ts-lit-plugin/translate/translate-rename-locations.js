"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var translate_range_1 = require("./translate-range");
function translateRenameLocations(renameLocations) {
    return renameLocations.map(function (renameLocation) { return translateRenameLocation(renameLocation); });
}
exports.translateRenameLocations = translateRenameLocations;
function translateRenameLocation(_a) {
    var fileName = _a.fileName, prefixText = _a.prefixText, suffixText = _a.suffixText, range = _a.range;
    var textSpan = translate_range_1.translateRange(range);
    return {
        textSpan: textSpan,
        fileName: fileName,
        prefixText: prefixText,
        suffixText: suffixText
    };
}
