#!/usr/bin/env node

// These are the node packages we are going to wrap.
var packages = {
    fs: {}
    os: {}
};

Object.keys(packages).forEach(function(pkgname) {
    var m = require(pkgname);
    Object.keys(m).forEach(function(funcname) {
        console.log(pkgname+'.'+funcname);
    });
});
