import { Document, Fragment, FragmentDetails } from '../../api';
/**
 * Represents a text document that contains a svelte component.
 */
export declare class SvelteDocument extends Document {
    url: string;
    content: string;
    script: Fragment;
    style: Fragment;
    constructor(url: string, content: string);
    /**
     * Get text content
     */
    getText(): string;
    /**
     * Set text content and increase the document version
     */
    setText(text: string): void;
    /**
     * Returns the file path if the url scheme is file
     */
    getFilePath(): string | null;
    getURL(): string;
    getAttributes(): {};
}
export declare class SvelteFragment implements FragmentDetails {
    document: SvelteDocument;
    tag: 'style' | 'script';
    private info;
    private version;
    constructor(document: SvelteDocument, tag: 'style' | 'script');
    readonly start: number;
    readonly end: number;
    readonly container: FragmentDetails['container'];
    readonly attributes: Record<string, string>;
    /**
     * Find the tag in the document if we detected a change
     */
    private update;
}
