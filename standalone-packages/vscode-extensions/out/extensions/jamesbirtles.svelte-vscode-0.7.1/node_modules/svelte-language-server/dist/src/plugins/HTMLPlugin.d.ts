import { Host, Document, HoverProvider, Position, Hover, CompletionsProvider, CompletionItem, FormattingProvider, TextEdit, SymbolInformation } from '../api';
export declare class HTMLPlugin implements HoverProvider, CompletionsProvider, FormattingProvider {
    pluginId: string;
    defaultConfig: {
        enable: boolean;
        hover: {
            enable: boolean;
        };
        completions: {
            enable: boolean;
        };
        tagComplete: {
            enable: boolean;
        };
        format: {
            enable: boolean;
        };
        documentSymbols: {
            enable: boolean;
        };
    };
    private host;
    private lang;
    private documents;
    onRegister(host: Host): void;
    doHover(document: Document, position: Position): Hover | null;
    getCompletions(document: Document, position: Position): CompletionItem[];
    doTagComplete(document: Document, position: Position): string | null;
    formatDocument(document: Document): TextEdit[];
    getDocumentSymbols(document: Document): SymbolInformation[];
}
