import { DiagnosticsProvider, Document, Diagnostic, Fragment, HoverProvider, Position, Hover, FormattingProvider, TextEdit, OnRegister, Host, DocumentSymbolsProvider, SymbolInformation } from '../api';
export declare class TypeScriptPlugin implements DiagnosticsProvider, HoverProvider, FormattingProvider, OnRegister, DocumentSymbolsProvider {
    static matchFragment(fragment: Fragment): boolean;
    pluginId: string;
    defaultConfig: {
        enable: boolean;
        diagnostics: {
            enable: boolean;
        };
        hover: {
            enable: boolean;
        };
        format: {
            enable: boolean;
        };
    };
    private host;
    private createDocument;
    onRegister(host: Host): void;
    getDiagnostics(document: Document): Diagnostic[];
    doHover(document: Document, position: Position): Hover | null;
    formatDocument(document: Document): Promise<TextEdit[]>;
    getDocumentSymbols(document: Document): SymbolInformation[];
}
