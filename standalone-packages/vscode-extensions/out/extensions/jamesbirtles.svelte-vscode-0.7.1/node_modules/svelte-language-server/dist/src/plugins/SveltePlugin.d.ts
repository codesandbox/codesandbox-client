import { DiagnosticsProvider, Document, Diagnostic, Host } from '../api';
export declare class SveltePlugin implements DiagnosticsProvider {
    pluginId: string;
    defaultConfig: {
        enable: boolean;
        diagnostics: {
            enable: boolean;
        };
    };
    private host;
    onRegister(host: Host): void;
    getDiagnostics(document: Document): Promise<Diagnostic[]>;
    private loadConfig;
}
