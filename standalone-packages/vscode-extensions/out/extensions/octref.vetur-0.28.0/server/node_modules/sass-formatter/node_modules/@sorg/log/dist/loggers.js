"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", { value: true });
var utils_1 = require("./utils");
var _1 = require(".");
/**Logs a table in node. */
function LogTable(table) {
    if (table[0] === undefined)
        return;
    var output = '';
    var maxLengths = [];
    for (var i = 0; i < table[0].length; i++) {
        var column = utils_1.getColumn(table, i);
        maxLengths.push(utils_1.getMaxLength(column));
    }
    for (var i = 0; i < table.length; i++) {
        var row = table[i];
        for (var j = 0; j < row.length; j++) {
            var field = row[j];
            output += field
                .toString()
                .concat(' '.repeat(maxLengths[j] - utils_1.replaceStyles(field).length + 1));
        }
        output += '\n';
    }
    console.log(output.replace(/\n$/, ''));
}
exports.LogTable = LogTable;
function Log() {
    var messages = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        messages[_i] = arguments[_i];
    }
    var output = '';
    var browserStyles = [];
    for (var i = 0; i < messages.length; i++) {
        var msg = messages[i];
        if (typeof msg === 'object') {
            if (utils_1.isBrowser) {
                browserStyles.push(utils_1.getBrowserStyle(msg.style));
            }
            output += _1.styler(msg.message, msg.style);
        }
        else {
            output += msg;
        }
        if (i < messages.length - 1) {
            output += ' ';
        }
    }
    console.log.apply(console, __spreadArrays([output], browserStyles));
}
exports.Log = Log;
