import { PresetHandler, CustomHandlerData } from './interfaces';
export declare class Preset {
    private func;
    constructor(func: PresetHandler<any>);
    Use(data: CustomHandlerData): string;
}
export declare class PresetNodeHelp extends Preset {
    text?: string | undefined;
    splitter: string;
    firstColumnWidth: number;
    secondColumnWidth: number;
    constructor(text?: string | undefined, splitter?: string, firstColumnWidth?: number, secondColumnWidth?: number);
}
