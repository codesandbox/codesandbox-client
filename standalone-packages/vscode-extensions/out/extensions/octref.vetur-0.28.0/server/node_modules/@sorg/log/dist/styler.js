"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var handleStyle_1 = require("./handleStyle");
var utils_1 = require("./utils");
exports.styler = function (input, style, wrapper) {
    if (typeof input === 'string') {
        input = { message: input, styled: false };
    }
    if (input.styled || (utils_1.isBrowser && input.nodeOnly)) {
        if (input.wrap) {
            return "" + handleStyle_1.WrapStyle(input.message, wrapper);
        }
        return input.message;
    }
    else {
        if (utils_1.isBrowser) {
            return "%c" + handleStyle_1.WrapStyle(input.message, wrapper);
        }
        else {
            if (style) {
                var NodeStyle = handleStyle_1.getNodeStyle(style);
                return handleStyle_1.WrapStyle(input.message, wrapper, "" + NodeStyle.color + NodeStyle.background + NodeStyle.bold, typeof style !== 'string' ? style.removeResetColorCode : false);
            }
            else {
                return handleStyle_1.WrapStyle(input.message, wrapper);
            }
        }
    }
};
