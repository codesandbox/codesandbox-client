version: 2

jobs:
  deploy:
    docker:
      - image: circleci/node:9.2.1
    working_directory: ~/codesandbox-client
    steps:
      - add_ssh_keys:
          fingerprints:
            - "f7:f1:e6:60:96:24:d9:cd:1b:8b:c0:34:e7:ee:fa:82"
      - deploy:
          command: ssh nginx@ssh.codesandbox.io "echo test; ls"

workflows:
  version: 2
  build-and-test:
    jobs:
      - deploy
