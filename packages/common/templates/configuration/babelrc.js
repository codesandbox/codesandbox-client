// @flow
import type { Sandbox } from 'common/types';

import Configuration from './configuration';

export default class BabelRCConfig extends Configuration {
  generateFileFromState(sandbox: Sandbox) {}
}
