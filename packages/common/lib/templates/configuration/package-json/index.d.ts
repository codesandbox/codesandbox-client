import { Sandbox } from '../../../types';
import { ConfigurationFile } from '../types';
export declare function generateFileFromSandbox(sandbox: Sandbox): string;
declare const config: ConfigurationFile;
export default config;
