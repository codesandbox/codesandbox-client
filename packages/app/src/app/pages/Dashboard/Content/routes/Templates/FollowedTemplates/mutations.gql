mutation unbookmarkTemplatee($template: ID!, $team: ID) {
  unbookmarkTemplate(templateId: $template, teamId: $team) {
    id
    bookmarked {
      isBookmarked
      entity {
        __typename
        ... on User {
          id
          name: username
        }
        ... on Team {
          id
          name
        }
      }
    }
  }
}
