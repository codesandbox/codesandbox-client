query getSandboxInfo($id: ID!) {
  sandbox(sandboxId: $id) {
    id
    author {
      id
      name: username
    }
    customTemplate {
      id
      bookmarked {
        isBookmarked
        entity {
          __typename

          ... on Team {
            id
            name
          }
        }
      }
    }
  }
}
