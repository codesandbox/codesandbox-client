import styled from 'styled-components';

const Badge = styled.p`
  border-radius: 2px;

  width: 28px;
  height: 12px;

  text-align: center;
  line-height: 12px;
  font-size: 9px;
  font-weight: 500;
`;

export { Badge };
